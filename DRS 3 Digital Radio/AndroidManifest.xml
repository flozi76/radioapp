<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.icaros.drs3"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="15" />

    <uses-configuration />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="Drs3OnAirService"
            android:icon="@drawable/ic_launcher"
            android:permission="android.permission.INTERNET" >
            <intent-filter>
                <action android:name="ch.icaros.drs3.Drs3OnAirService" />
            </intent-filter>
        </service>

        <activity
            android:name=".SongList"
            android:label="@string/title_activity_song_list">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ch.icaros.drs3.MainActivity" />
        </activity>
		  <activity android:name="com.google.ads.AdActivity"
                      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"/>
    </application>

</manifest>